import stdlib.prelude;

pub type Vec3 {
    x: float,
    y: float,
    z: float,
}

pub type Vec2 {
    x: float,
    y: float,
}

pub internal def distance(self: Vec2, other: Vec2) -> float {
    return ((other.x - self.x).pow(2) + (other.y - self.y).pow(2)).sqrt();
}